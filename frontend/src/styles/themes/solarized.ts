import { snzPalette } from "./snz";

// The shared snz-design Solarized pair replaces the earlier 1:1 port of the
// original single theme (teal accent, translucent surfaces), which stays in
// git history. Existing Solarized users see the shared accent from here on.
export const solarized = snzPalette("solarized");
