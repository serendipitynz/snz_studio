# AGENTS.md（日本語版）

## 目標
ローカル LLM 向けの最小構成シングルユーザー・プロジェクトワークスペースを構築する。UX は ChatGPT / Claude Projects に近い感覚を目指す。

## 優先順位
1. 実装スピード
2. シンプルさ
3. 可読性
4. ローカル完結運用
5. 将来の拡張のしやすさ

## コアとなる製品構造
アプリは以下をサポートする：
- プロジェクト
- ドキュメント
- チャット
- メモリ

各チャットはプロジェクトに属する。
各プロジェクトには複数のドキュメントとメモリを持てる。
チャットはプロジェクトのドキュメントとメモリをコンテキストとして利用できる。

## 必要な UX
ローカル版 ChatGPT Projects を軽量に再現した UX を目指す：
- 各プロジェクトは共有コンテキストを持つ
- 1 つのプロジェクトに複数のチャットを作成できる
- チャット中にプロジェクトドキュメントを参照できる
- 過去のチャット履歴はサマリーで圧縮する
- 永続的なメモリは生メッセージとは別に保存する
- UI には回答で参照した情報源を表示する

## 制約
- シングルユーザーのみ
- ローカルマシン限定
- 認証なし
- マルチユーザー非対応
- PDF 非対応
- OCR 非対応
- ベクトル DB 不使用
- Electron 不使用
- 外部インフラ不使用
- 重いリアルタイムアーキテクチャ不使用
- 過度な設計禁止
- Tailwind 不使用

## 技術スタック
- デスクトップシェル: Wails v2（Go コア + OS ネイティブ WebView）
- フロントエンド: React + Vite + TypeScript（Tailwind なし）
- バックエンド: Go ローカル API サーバー（loopback の `net/http`、SSE 温存）
- データベース: SQLite（`modernc.org/sqlite`、pure Go）
- ファイルストレージ: ローカルファイルシステム

実装を明らかに簡略化できる場合に限り、軽量でメジャーなライブラリを使用する。

## 検索・検索（Retrieval）
ハイブリッドアプローチを採用：SQLite FTS5 によるキーワードマッチングと、オプションの Embedding ベースセマンティック検索（ベクトルは SQLite に JSON で保存）を組み合わせる。
Retrieval 層は `internal/service/retrieval.go` に分離されており、再ランキングやクロスエンコーダーなどの追加戦略をチャットロジックに触れずに組み込める。
Embedding はオプトイン方式（`EMBEDDING_MODEL` が未設定の場合は無効）。

## ドキュメント
以下のドキュメントタイプをサポートする：
- markdown
- text
- image

画像の場合は以下を保存する：
- ファイル
- タイトル
- ノート
- タグ
- derived_text（派生テキスト）

検索はテキストコンテンツ・ノート・タグ・derived_text に対して動作する。

## メモリ
チャットをまたいで有用な恒久的な事実のみを保存する。
一時的な雑談はメモリに保存しない。
シンプルに保ちつつ、以下のカテゴリが存在できる構造にする：
- semantic（意味的知識）
- procedural（手続き的知識）
- episodic（エピソード的知識）

## UI
必須画面：
- プロジェクト一覧
- プロジェクト詳細
- チャット画面
- ドキュメント / 画像追加フロー

UI はシンプルで構わないが、快適に使えるものにする。
CLI のみの UX は不可。

## 実装スタイル
- 最小動作バージョンから始める
- 抽象化より明快なコードを優先する
- 不要なレイヤーを避ける
- ファイルは適度な大きさに保つ
- 直感的な命名を使う
- 将来の拡張ポイントに簡潔なメモを残す

## 成果物
以下を含める：
- 動作するアプリ
- README
- サンプル env ファイル
- サンプルシードデータ
- 基本的なセットアップ手順
