---
id: TASK-34
title: '多人数会話: 発言本文の呼びかけを重みに織り込むターン規則 weighted を追加する'
status: To Do
assignee: []
created_date: '2026-09-21 22:56'
updated_date: '2026-09-22 04:10'
labels: []
dependencies:
  - TASK-33
references:
  - docs/multi-agent-chat-design.md
  - internal/service/turnengine.go
  - internal/model/model.go
  - internal/db/migrations.go
  - internal/preset/bundled
  - frontend/src/components/ParticipantPanel.tsx
type: feature
ordinal: 34000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## 背景

「呼ばれた人が答える」会話は既存の 3 規則では出ない。TASK-28 の spike が、スコア方式 (`weighted`) と、
呼びかけ先を最優先する順位規則 (`addressed_first`) を 60 ターンのシミュレーションで比較し、
**`weighted` を採用**して `docs/multi-agent-chat-design.md` §4.6 に記録した (ユーザー判断)。

**呼びかけは「末尾のメッセージ 1 つ」ではなく「直近 n 参加者発言の窓のうち未応答のあいだ」効く加点として表す**
(2026-09-22 のユーザー提案)。起票時の原案「呼びかけ先以外を ×0.5」から替えた理由は §4.6.4 にあり、
要点は (1) 原案は順位としては ×2.0 に当たり呼びかけ先が必ず勝つ、(2) 末尾 1 つしか読まないので
`[A, B]` の A が話した瞬間に B への呼びかけが消え、応答されない呼びかけが残る、の 2 点。
窓つきの ×1.2 は未応答を 0 にしつつ一巡ターン数と発言の偏りも改善する (§4.6.3 読み取り 2・4)。

窓を与えた順位規則とは測定上互角で傾向が違う (順位規則は即時追従で勝ち、`weighted` は一巡ターン数・
未応答・発言の偏りで勝つ)。採った理由は呼びかけを他の圧力と競合させられることにある (§4.6.3 読み取り 6)。

**呼びかけ**とは、ある発言の本文が、次に話す相手として編成の参加者 1 人以上の表示名を挙げていることを指す。
`manual` の人間による指名とも、§7「将来」の進行役による指名とも別物である。

## やること (設計は §4.6.1 / §4.6.4 / §4.6.5 / §4.6.7)

- migration で、発言に紐づく呼びかけ先の参加者 id の**集合**を保存する場所を足す (0 個 = 呼びかけ無し)。
- 呼びかけの検出を発言の**保存時**に確定して書く。後から本文を読み直す形にしない (§2)。窓のあいだ何度も
  読み返されるので、再解釈のたびに答えが変わる余地を残さない。判定のための追加の LLM 呼び出しはしない。
  - (a) 役割リマインド (§4.3) に `[次: 表示名]` の末尾指示子を足し (複数なら読点区切り)、保存前に末尾の 1 行だけ
    剥がして id に解決する。編成に一致しない表示名はその 1 件だけ捨てる。一致 0 件なら呼びかけ無しとしたうえで
    指示子は剥がす (本文に制御用の記法を残さない)。
  - (b) (a) が空振りしたときだけ名前の照合。最終文に現れた編成の表示名のうち自分自身を除いた全員を呼びかけ先とする。
    最終文に限り、表示名 2 文字以上に限る。**編成の全員が一致しても捨てない** (§4.6.5。加点の形では
    捨てた場合と残した場合で発言順が変わるので、特別扱いを置かない)。
  - 人間の介入発言にも (b) をかける (`POST /messages` 側)。
- `turn_rule` に 4 つ目の値 `weighted` を足す。ターン開始時に編成の参加者ごとに重み (係数の積) を求め、最大が話す:

  | 係数 | 掛かる相手 | 値 |
  | --- | --- | --- |
  | 連続発言の抑制 | 末尾のメッセージを書いた参加者。末尾が人間の介入発言なら誰にも掛からない | ×0.2 |
  | 直近発言の抑制 | 直近 n ターン (n = 編成の人数) に発言した参加者。進行役は対象外 | ×0.85 |
  | 未応答の呼びかけの加点 | 未応答の呼びかけを持つ参加者 | ×1.2 |
  | 名簿順の維持 | 直近の参加者発言の次に当たる参加者以外の全員 | ×0.95 |

  - **呼びかけの窓** = 直近 n 件の参加者発言 (n = 編成の人数)。人間の介入発言は窓を消費しないが、
    介入が**した**呼びかけは読む。
  - **未応答の呼びかけ** = 窓のうちに呼びかけられ、その呼びかけより後にまだ発言していない状態。
    **応答したら加点は消える** (§4.6.3 読み取り 5)。
  - 同点規則は「沈黙が長い側 → なお同点なら名簿順の先」。未発言の参加者は最も沈黙が長いものとして扱う
    (窓を入れても同点は残るので必須、§4.6.3 読み取り 7)。
  - 正規化 (合計 1) は表示のときだけ行う。
  - **人間の介入発言の後に進行役を強制する上書きは入れない** (§4.6.7)。
- 編成パネルのターン進行ルール選択に `weighted` を足し、1 行の説明を添える。
- TASK-33 の `speaker` イベントに重みの内訳を載せる (フロント側に算術を足さない)。

## 加点の値は測定で選んだもの (§4.6.1 / §4.6.3 読み取り 1)

`1/0.85 ≈ 1.176` と `1/(0.85 × 0.95) ≈ 1.238` はどちらも「進行役の免除に勝てるか」から出る**保証の境界**で、
挙動が切り替わる点ではない。下回っても勝つ場合があり、上回っても窓は働き続ける。
×1.2 を採ったのは測定の結果である: 進行役が居る編成で ×1.1 は呼びかけをほぼ通さず (即時追従 5/18、
加点なしの 5/19 とほぼ同じ)、×1.3 以上は複数呼びかけで未応答を 2 件作り、**×1.2 だけが未応答 0** になる。
進行役が居ない編成ではこの境界が効かない (3 人・進行役なしでは ×1.1 でも即時追従 13/13)。
**値を動かしたら `tools/turn-score-sim` で測り直す。** 編成の形によって最適値が違いうる。

## 範囲外

- **`talkativeness` (参加者ごとの発言頻度) は入れない** (§4.6.10)。決定的なエンジンでは定数係数に
  なるしかなく、値の低い参加者が構造的に締め出される (係数 0.2 の参加者は 60 ターンで一巡に加わらない)。
  入れるなら沈黙を連続量にする変更と抱き合わせが必要で、それは規則の形の変更になる。
  なお進行役の ×0.85 免除は `talkativeness` 1.15 と測定上等価で、すでにある特殊ケースである。
- **応答者リスト方式 (1 リクエストで複数人が話す) は移さない** (§4.6.10)。1 リクエスト = 1 ターンは §4.1 の決定。

## 前提

既存の `round_robin` / `manual` / `facilitator_alternating` の振る舞いは**一切変えない**。呼びかけ追従を
chat 単位の真偽値設定にして既存 3 規則すべてに効かせる形は採らない (§4.6.4)。

## 検証シナリオ

- 同梱プリセット `trpg-table` (GM + プレイヤー 2 名) を `weighted` で回し、GM が名指ししたプレイヤーが次に話すこと、
  名指しの無いターンは進行役が挟まる形になることを実機で確認する。
- 1 発言が 2 人を呼ぶケースで、**両方が窓のうちに話す**ことを確認する (未応答が残らないこと)。
- 人間が直前の話者を名指しで呼んだとき、その participant が**呼びかけの窓のうちに**話すことを確認する
  (即時とは限らない。進行役が名簿順の次に当たると 1 ターン遅れる、§4.6.7)。
- 小型モデルが `[次: 表示名]` をどの程度書くか (検出 (a) の当たり方) を実機で測り、(b) のフォールバックが
  どれだけ働くかを記録する。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 発言に紐づく呼びかけ先の参加者 id の集合が保存され、1 発言が複数を呼べる。検出結果は発言の保存時に確定する (保存後に本文を読み直さない)
- [ ] #2 末尾指示子と名前照合の 2 段で呼びかけを検出し、指示子は本文に残らない。人間の介入発言にも名前照合がかかり、自分自身への呼びかけと編成全員への一致は呼びかけ無しになる
- [ ] #3 `turn_rule` = `weighted` のとき §4.6.1 の 4 係数の積で重みが決まり、最大の参加者が話す。呼びかけの加点は直近 n 参加者発言の窓のうち未応答のあいだ掛かり、応答すると消えることがテストで確認されている
- [ ] #4 同点は「沈黙が長い側 → 名簿順」で割れ、未発言の参加者が最も沈黙が長いものとして扱われることがテストで確認されている
- [ ] #5 既存の round_robin / manual / facilitator_alternating の話者選択が変わっていないことがテストで確認されている
- [ ] #6 編成パネルから `weighted` を選べ、同梱プリセット trpg-table と、1 発言が 2 人を呼ぶケース (両方が窓のうちに話す) が実機で確認できる
- [ ] #7 人間の介入発言が末尾のとき連続発言の抑制が誰にも掛からないこと、および人間が直前の話者を名指しで呼んだとき その participant が呼びかけの窓のうちに話すこと (次のターンとは限らない) がテストで確認されている
<!-- AC:END -->
