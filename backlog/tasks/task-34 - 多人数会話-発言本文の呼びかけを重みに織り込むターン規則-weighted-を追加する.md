---
id: TASK-34
title: '多人数会話: 発言本文の呼びかけを重みに織り込むターン規則 weighted を追加する'
status: In Review
assignee: []
created_date: '2026-09-21 22:56'
updated_date: '2026-09-22 06:42'
labels: []
dependencies:
  - TASK-33
references:
  - docs/multi-agent-chat-design.md
  - internal/service/turnengine.go
  - internal/model/model.go
  - internal/db/migrations.go
  - internal/preset/bundled
  - frontend/src/components/ParticipantPanel.tsx
type: feature
ordinal: 34000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## 背景

「呼ばれた人が答える」会話は既存の 3 規則では出ない。TASK-28 の spike が、スコア方式 (`weighted`) と、
呼びかけ先を最優先する順位規則 (`addressed_first`) を 60 ターンのシミュレーションで比較し、
**`weighted` を採用**して `docs/multi-agent-chat-design.md` §4.6 に記録した (ユーザー判断)。

係数は **3 つ**。起票時の原案から 2 点変えてある。

1. **呼びかけは「末尾のメッセージの呼びかけ先以外を ×0.5」ではなく「直近 n 参加者発言の窓のうち
   未応答の呼びかけを持つ参加者に ×1.2」**。原案は末尾 1 つしか読まないので、`[A, B]` の A が話した瞬間に
   B への呼びかけが消える (§4.6.4)。
2. **「名簿順の次以外を ×0.95」は置かない**。発言順を変えておらず (呼びかけの無い編成では戻しても全指標が
   一致し、変わるのは同点の回数だけ)、一方で呼びかけの必要加点を `1/0.85` から `1/(0.85×0.95)` に上げていた。
   戻すと ×1.2 では足りず即時追従率が 21/21 から 14/21 に落ちる (§4.6.3 読み取り 1)。

`addressed_first` を採らない理由は、呼びかけが常に他を先取りすると呼ばれない参加者が飢えること
(一巡ターン数 30、採用案は 8。§4.6.3 読み取り 6)。

**呼びかけ**とは、ある発言の本文が、次に話す相手として編成の参加者 1 人以上の表示名を挙げていることを指す。
`manual` の人間による指名とも、§7「将来」の進行役による指名とも別物である。

## やること (設計は §4.6.1 / §4.6.4 / §4.6.5 / §4.6.7)

- **まず `tools/turn-score-sim` にテストを書く。** 実装より先に行う。
  **Why**: TASK-28 の spike では、このツールの計測バグで結論が 3 回動いた。`tools/` に `_test.go` が
  1 つも無かったことが直接の原因である。このタスクは同じツールで挙動を検証するので、ツールが
  信用できないまま進めると同じことが起きる。最低限、実際に起きた 5 件を回帰ケースにする:
  - 話者選択が手計算と一致する (既知の履歴に対する重みの配列と、選ばれる話者)。
  - 同点規則で、一度も発言していない参加者が最も沈黙が長いものとして扱われる
    (`turnsSinceSpoken` の -1 を最短沈黙と読んでいた)。
  - 応答待ちと即時追従率が、打ち切り位置に当たった呼びかけを未応答に数えない
    (この補正が無いと、加点を強めた変種のほうが未応答が多く見える)。
  - 呼びかけの抽選が規則に依存しない (同一シナリオの全規則が同じ呼びかけ件数を相手にする)。
  - 未応答の呼びかけが応答で消え、窓を出ると消える (`outstandingCall` の窓の数え方。
    人間の介入発言は窓を消費しないが、介入がした呼びかけは読む)。
  設計書 §4.6.3 の表は `go run ./tools/turn-score-sim` の出力からの抜粋なので、
  ツールに手を入れたら表の数値を出力と突き合わせて更新する (目視ではなく機械的に照合する)。
- migration で、発言に紐づく呼びかけ先の参加者 id の**集合**を保存する場所を足す (0 個 = 呼びかけ無し)。
- 呼びかけの検出を発言の**保存時**に確定して書く。後から本文を読み直す形にしない (§2)。窓のあいだ何度も
  読み返されるので、再解釈のたびに答えが変わる余地を残さない。判定のための追加の LLM 呼び出しはしない。
  - (a) 役割リマインド (§4.3) に `[次: 表示名]` の末尾指示子を足し (複数なら読点区切り)、保存前に末尾の 1 行だけ
    剥がして id に解決する。編成に一致しない表示名はその 1 件だけ捨てる。一致 0 件なら呼びかけ無しとしたうえで
    指示子は剥がす (本文に制御用の記法を残さない)。
  - (b) (a) が空振りしたときだけ名前の照合。最終文に現れた編成の表示名のうち自分自身を除いた全員を呼びかけ先とする。
    最終文に限り、表示名 2 文字以上に限る。**編成の全員が一致しても捨てない** (§4.6.5)。
  - 人間の介入発言にも (b) をかける (`POST /messages` 側)。
- `turn_rule` に 4 つ目の値 `weighted` を足す。ターン開始時に編成の参加者ごとに重み (係数の積) を求め、最大が話す:

  | 係数 | 掛かる相手 | 値 |
  | --- | --- | --- |
  | 連続発言の抑制 | 末尾のメッセージを書いた参加者。末尾が人間の介入発言なら誰にも掛からない | ×0.2 |
  | 直近発言の抑制 | 直近 n ターン (n = 編成の人数) に発言した参加者。進行役は対象外 | ×0.85 |
  | 未応答の呼びかけの加点 | 未応答の呼びかけを持つ参加者 | ×1.2 |

  - **呼びかけの窓** = 直近 n 件の参加者発言 (n = 編成の人数)。人間の介入発言は窓を消費しないが、
    介入が**した**呼びかけは読む。窓の大きさは結果にほとんど影響しないので (§4.6.3 読み取り 3)、
    直近発言の抑制の窓と同じ値にして説明を 1 つに保つ。
    **窓が効くのは呼びかけ先が複数のときと人間の介入があるときだけ**で、呼びかけ先が 1 人のあいだは
    末尾のメッセージだけを読む形と 1 発言も違わない (§4.6.3 読み取り 9)。実装の検証はこの 2 つで行う。
  - **未応答の呼びかけ** = 窓のうちに呼びかけられ、その呼びかけより後にまだ発言していない状態。
    **応答したら加点は消える** (§4.6.3 読み取り 5)。
  - **同点規則は「沈黙が長い側 → なお同点なら名簿順の先」。未発言の参加者は最も沈黙が長いものとして扱う。**
    これがこの規則の中核である。係数が 3 つとも離散値なので重みが並ぶことが多く (60 ターン中 16〜21 回、
    進行役の居ない 3 人編成では 59 回)、そこで誰が先かは同点規則が決めている (§4.6.3 読み取り 4)。
    名簿順だけの同点規則にすると一巡ターン数 8 → 12、最大発言シェア 32% → 40% に悪化する。
  - 正規化 (合計 1) は表示のときだけ行う。
  - **人間の介入発言の後に進行役を強制する上書きは入れない** (§4.6.7)。
- 編成パネルのターン進行ルール選択に `weighted` を足し、1 行の説明を添える。
- TASK-33 の `speaker` イベントに重みの内訳を載せる (フロント側に算術を足さない)。

## 加点の値 (§4.6.1 / §4.6.3 読み取り 2)

呼びかけが越えなければならないのは重み 1.0 の参加者である。定常状態では編成のほとんどが ×0.85 を
受けているが、1.0 に居る参加者が 2 通りある — 免除された進行役 (常時) と、窓を越えて沈黙した参加者
(一時的に)。条件は **加点 > 1/0.85 ≈ 1.176** の一本になる。進行役が居ない編成でも、沈黙が窓を越えた
参加者が 1.0 に上がるので境界は無くならない。
×1.1 は下回るので呼びかけが通らない (即時追従率 11/21、加点なしと同値)。
**×1.2 / ×1.3 / ×2.0 は全シナリオで完全に一致する** (1.176 を越えた時点で飽和する)。
値を動かすなら 1.176 より上に限り、動かしたら `tools/turn-score-sim` で測り直す。

## 範囲外

- **`talkativeness` (参加者ごとの発言頻度) は入れない** (§4.6.10)。決定的なエンジンでは定数係数に
  なるしかなく、値の低い参加者が構造的に締め出される (係数 0.8 で一巡ターン数が 7 → 39、0.5 で一巡せず)。
  入れるなら沈黙を連続量にする変更と抱き合わせが必要で、それは規則の形の変更になる。
- **応答されなかった呼びかけを窓より先へ持ち越さない** (§4.6.4)。持ち越すには待ち行列が必要で、
  §2 の「サーバー側に進行状態を持たない」と両立させる導出の設計が別途要る。
- **応答者リスト方式 (1 リクエストで複数人が話す) は移さない** (§4.6.10)。1 リクエスト = 1 ターンは §4.1 の決定。

## 前提

既存の `round_robin` / `manual` / `facilitator_alternating` の振る舞いは**一切変えない**。呼びかけ追従を
chat 単位の真偽値設定にして既存 3 規則すべてに効かせる形は採らない (§4.6.4)。

## 検証シナリオ

- 同梱プリセット `trpg-table` (GM + プレイヤー 2 名) を `weighted` で回し、GM が名指ししたプレイヤーが次に話すこと、
  名指しの無いターンは進行役が挟まる形になることを実機で確認する。
- 1 発言が 2 人を呼ぶケースで、**両方が窓のうちに話す**ことを確認する。
- 人間が直前の話者を名指しで呼んだとき、その participant が**呼びかけの窓のうちに**話すことを確認する。
- 小型モデルが `[次: 表示名]` をどの程度書くか (検出 (a) の当たり方) を実機で測り、(b) のフォールバックが
  どれだけ働くかを記録する。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 発言に紐づく呼びかけ先の参加者 id の集合が保存され、1 発言が複数を呼べる。検出結果は発言の保存時に確定する (保存後に本文を読み直さない)
- [x] #2 同点は「沈黙が長い側 → 名簿順」で割れ、未発言の参加者が最も沈黙が長いものとして扱われることがテストで確認されている
- [x] #3 既存の round_robin / manual / facilitator_alternating の話者選択が変わっていないことがテストで確認されている
- [ ] #4 編成パネルから `weighted` を選べ、同梱プリセット trpg-table と、1 発言が 2 人を呼ぶケース (両方が窓のうちに話す) が実機で確認できる
- [x] #5 人間の介入発言が末尾のとき連続発言の抑制が誰にも掛からないこと、および人間が直前の話者を名指しで呼んだとき その participant が呼びかけの窓のうちに話すこと (次のターンとは限らない) がテストで確認されている
- [x] #6 末尾指示子と名前照合の 2 段で呼びかけを検出し、指示子は本文に残らない。一致した参加者は自分自身を除いて全員が呼びかけ先として保存され (編成の全員が一致した場合も捨てない)、人間の介入発言にも名前照合がかかることがテストで確認されている
- [x] #7 `turn_rule` = `weighted` のとき §4.6.1 の 3 係数の積で重みが決まり、最大の参加者が話す。呼びかけの加点は直近 n 参加者発言の窓のうち未応答のあいだ掛かり、応答すると消えることがテストで確認されている。名簿順の係数は持たない
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. tools/turn-score-sim/main_test.go を先に書く (手計算一致・未発言の同点・打ち切り補正・呼びかけ抽選の規則非依存・outstandingCall の窓)。ツールに手が入ったら §4.6.3 の表を出力と機械照合する。
2. migration 015: messages.addressed_participant_ids TEXT NOT NULL DEFAULT '[]' (JSON 配列)。model.Message / repository の scan・insert に通す。
3. service/addressing.go: 末尾指示子 [次: 表示名] の剥離と解決 (a)、最終文の名前照合 (b)。保存時に確定。
   - 検出と保存は多人数会話の全発言 (規則によらず) で行う。規則を後から weighted に切り替えても窓が読める。
   - 役割リマインドへの指示子の追記は weighted のときだけ (既存 3 規則のプロンプトを変えない)。
   - 人間の介入 (storeHumanMessage) にも (b)。
4. turn_rule に weighted を追加 (model / PATCH 検証 / preset 検証 / export 表記)。進行役は facilitator_participant_id を共用 (直近発言の抑制の免除対象)。
5. weightedSpeaker: 3 係数の積・同点は沈黙が長い側→名簿順・未発言は最長沈黙。speaker イベントの weights に内訳 (係数名はキー、表示名も載せる)。
6. フロント: 規則の選択肢 + 1 行説明、weighted でも進行役を選べる、係数名の i18n。
7. 設計書 §2/§3/§4.6/§5/§6/§7 と current-spec / README を実装に合わせて更新。
8. go test / go vet / pnpm check:client、実機で trpg-table・2 人呼び・人間の名指しを確認。
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## 実装
- migration 015: messages.addressed_participant_ids (JSON 配列 TEXT, 既定 '[]')。結合テーブルにしないのは集合が発言と一緒に丸ごと読まれるだけのため。外部キーは facilitator_participant_id と同じ理由で張らない。
- 検出 (internal/service/addressing.go): (a) 末尾 1 行の [次: 名前] を剥がして解決 → 指示子があればそれで確定 (0 件一致でも剥がす)、(b) 指示子が無いときだけ最終文の名前照合 (2 文字以上・自分を除く・全員一致も保持)。人間の介入は (b) のみ (storeHumanMessage)。
- 検出・保存は規則によらず多人数会話の全発言で行う。役割リマインドへの指示子の追記は weighted のときだけ (既存 3 規則のプロンプトは不変)。途中で weighted に切り替えても窓が保存済みの呼びかけを読める。
- 指示子しか無い発言は ErrUtteranceOnlyDirective でターン失敗 (空の発言を保存しない §4.3 と揃える)。
- 規則 (internal/service/turnweighted.go): 3 係数 (consecutive ×0.2 / recent ×0.85 / call ×1.2)、同点は沈黙が長い側 → 名簿順、未発言は最長沈黙。進行役は facilitator_participant_id を共用し、weighted では任意 (未選択なら免除なし)。プリセットも weighted では facilitator 0〜1 件を許す。
- speaker イベントの weights に全員分の内訳を載せ、SpeakerWeight に displayName を足した。係数名はキーで送り、フロントが i18n で表示名にする。

## 着手時に決めたこと (設計書 §4.6.5「実装で決めた細部」に記録)
- **参加者を指す名前は「表示名」と「表示名の末尾の括弧書きを除いた部分」の 2 つ。** 実機で trpg-table の GM が [次: レン, ミラ] と書き (表示名は「レン (斥候)」)、完全一致では呼びかけが全部捨てられたため。

## 検証
- tools/turn-score-sim/main_test.go: 回帰 5 件 (手計算一致・未発言の同点・打ち切り補正・呼びかけ抽選の規則非依存・outstandingCall の窓)。各バグを一時的に戻すと該当テストが落ちることを確認。ツール出力は変更前と完全一致 (diff 0 行) なので §4.6.3 の表は更新不要。
- go test ./... / go vet ./... / pnpm check:client すべて通過。
- AC#3: 既存テストはすべて無修正で通過 + round_robin / facilitator_alternating が保存済みの呼びかけを無視するテストを追加。
- 実機 (LM Studio, google/gemma-4-e4b, 一時 DB で HTTP API 経由):
  - trpg-table を weighted で 12 ターン: GM の名指しに追従、名指しの無いターンの次は GM。GM が [次: レン, ミラ] と 2 人を呼んだ発言では T2 レン・T3 ミラと両方が窓のうちに話した。
  - 人間が直前の話者 (GM) を名指し → 次のターンで GM (×0.2 が掛からず ×1.2)。
  - 指示子 (a) の当たり: trpg 12 ターン中 7 回書かれ、名前入りの 6 回はすべて解決。残りは空の [次: ] (剥がして呼びかけ無し)。(b) が効いたのは 2 回。4 人討論 (司会 + 3) 16 ターン: 12 回書かれ 11 回解決 (1 回は空)、(b) 1 回、複数呼びかけ 5 回。本文に指示子が残った発言は 0。
  - gemma は「いなければ書かない」の指示にかかわらず空の [次: ] を書く。剥がすので実害は無い。

## 未確認 (AC#4 を未チェックにした理由)
- 編成パネルから weighted を選ぶ操作は Wails のネイティブウィンドウを操作できず未確認。型チェックとコードでは選択肢と説明を足してある。pnpm dev で選択 → trpg-table を回す確認をユーザーに依頼する。
<!-- SECTION:NOTES:END -->
