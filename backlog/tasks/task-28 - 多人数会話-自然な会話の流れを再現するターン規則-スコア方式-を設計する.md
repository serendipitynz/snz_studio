---
id: TASK-28
title: '多人数会話: 自然な会話の流れを再現するターン規則 (スコア方式) を設計する'
status: In Review
assignee: []
created_date: '2026-09-21 03:23'
updated_date: '2026-09-22 05:27'
labels: []
dependencies: []
references:
  - docs/multi-agent-chat-design.md
  - internal/service/turnengine.go
  - internal/model/model.go
  - frontend/src/api/turnOrder.ts
  - frontend/src/components/ParticipantPanel.tsx
type: spike
ordinal: 28000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## 背景

ターン規則は現在 `round_robin` と `manual` で、TASK-21 が「進行役を 1 人おきに挟む」規則を足す。
いずれも順番が固定で、実際の会話にある「呼ばれた人が答える」「しばらく黙っていた人に話が回る」
「同じ人が続けて話さない」といった流れは出ない。この spike では、そうした自然な会話の流れを
決定的なロジックとして表現できるかを設計し、実装タスクを切るかを判断する。既存の 3 規則は置き換えず、
第 4 の規則として並置する前提で検討する。

## 出発点となる案 (スコア方式)

ターン開始時に参加者ごとの重みを計算し、最大の参加者が話す。重みは次の規則の係数の積と定義する。原案の「減った分を他の参加者に按分する」操作は
係数の積と等価ではなく (等重みの 3 人で A を ×0.2 して比例按分すると 1:7:7、積を正規化すると 1:5:5)、
按分は規則の適用順序にも依存する。順序に依存せず説明しやすい積の定義を採り、合計 1 の正規化は表示用にだけ行う:

- 現ターンの発言者 ×0.2 (同じ人が続けて話さない)
- 沈黙優遇: 参加者人数ターン以上発言していない参加者を相対的に上げる (原案では直近 n ターンに発言した
  非進行役を ×0.85。定常状態ではほぼ全員に当たるので「沈黙者を上げる」と書くほうが読みやすい)
- 指名: 直近の発言が次の話者を名指ししていれば、それ以外を ×0.5
- 名簿順の次の参加者以外を ×0.95 (何もなければ round_robin に近づける)
- 進行役 (任意): 沈黙優遇の対象外にする

原案の係数で積を取ると、指名も長い沈黙も無いあいだは、直近に発言した非進行役は最大 0.85、進行役は現ターン発言者で
ない限り 0.95 以上なので、進行役が 1 ターンおきに挟まる (TASK-21 に近い振る舞いが係数から出る)。ただし保証ではない:
指名があれば進行役も ×0.5 を受ける (名簿 GM, A, B, C で直前発言者 A が B を指名すると GM 0.475 < B 0.85)。
長く沈黙した参加者 (1.0) も進行役 (0.95) に勝つ。TASK-21 の確定的な交互進行の代替にはならないので、並置が前提。
また名簿 GM, A, B, C で直前発言者が A、名簿順の次 B が直近に発言済み、C が長く沈黙、のとき C と GM が
0.95 で並ぶ。同点規則が必要。

この案が唯一の候補ではない。自然な流れを別の形 (例: 規則の優先順位付きの決定木) で表現できるなら
そちらでもよく、比較して選ぶ。

## 制約 (設計書 §2 を保つ)

- 次の話者は名簿と保存済み発言だけから導出し、サーバー側に進行状態を持たない (再起動・複数ウィンドウで同じ答え)。
- 指名の検出結果は発言の保存時に確定して発言側に保存する (`nominated_participant_id` 相当)。後から本文を
  再解釈すると再起動前後で答えが変わり得る。
- 判定のための追加の LLM 呼び出しはしない (設計書 §8.1 が履歴要約を見送った理由と同じ: 小型モデルで 1 ターンの遅延が倍になる)。
- 人間の介入発言は名簿にいないので、「名簿順の次」は直近の参加者発言から取り、指名の検出は直近の発言 (人間含む)
  に対して行う。名指しの無い介入の後に誰が話すかは決めること 5 に含める: 直前の参加者発言を「現ターンの発言者」として
  ×0.2 を課すと、進行役が話した直後に人間が介入した場合に進行役が応じられない (GM 0.19 < 名簿順の次 0.85)。

## このタスクで決めること (実装はしない)

1. 規則の集合と係数、同点規則。3〜4 人 (進行役あり/なし) で数十ターンを回す簡単なシミュレーション
   (スクリプトでよい) で、同じ人の連続、全員に発言が回るまでのターン数、進行役の挟まり方を確認して決める。
   指名がある場合と、名指しの無い人間の介入の後の場合をシナリオに含める。
2. 指名の検出方式。候補: (a) 役割リマインダーで末尾に `[次: 表示名]` を書かせ、保存前に剥がして記録する
   構造化出力 (設計書 §4.3 のとおり小型モデルは書式を模倣しやすい。剥がし損ねの防御が要る)、
   (b) 直近発言の末尾文に他の参加者の表示名 (+「さん」「?」など) があれば指名とみなす名前照合
   (三人称の言及を誤検出する。(a) の空振り時のフォールバック向き)。推奨は (a) + (b) のフォールバック。
3. 次話者表示の出し方。フロントの `predictNextSpeaker` (`turnOrder.ts`) は round_robin を複製しているが、
   この方式は算術を二重に持つべきでないので、サーバーが次話者と重みの内訳を返す形を検討する。
4. 実装タスクを切るか。切る場合は TASK-21 の TRPG 卓プリセットを検証シナリオの 1 つにする。
5. 名指しの無い人間の介入発言の後の話者。候補: 人間の発言を「現ターンの発言者」とみなして参加者の誰にも ×0.2 を
   課さない (進行役が沈黙優遇の対象外である分だけ応じやすくなる)、または介入後は進行役を優先する明示規則。
   TASK-21 が設計時に決める「人間の介入発言を挟んだ後に誰が話すか」と揃える。

各案の利点・欠点と推奨を設計書 `docs/multi-agent-chat-design.md` に追記する (§8.2 の「進行役モデルによる
発言者指名」の項と整合させる)。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 規則の集合・係数・同点規則について、シミュレーション結果を根拠に案と推奨が設計書に記録されている
- [x] #2 指名の検出方式と保存形式 (発言側に確定して保存する) について、案と推奨が設計書に記録されている
- [x] #3 次話者と重みの内訳をフロントにどう見せるか (サーバーが返す形を含む) が設計書に記録されている
- [x] #4 推奨案の実装タスクが起票されている (または実装不要と判断した根拠が記録されている)。既存の round_robin / manual / TASK-21 の規則は置き換えない前提で書かれている
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. 対応表 (referent table) を先に確定する (semantic-generation)。スコア方式の語彙: 重み・係数・沈黙・指名・同点規則・判定文。
2. tools/turn-score-sim/ に決定的シミュレータ (Go, 追加依存なし) を置き、3〜4 人 (進行役あり/なし)・数十ターンで、同じ人の連続・全員一巡までのターン数・進行役の挟まり方を測る。指名あり / 名指し無しの人間介入後をシナリオに含める。原案の係数と対案を同じスクリプトで比較する。
3. 測定結果を根拠に、規則集合・係数・同点規則を決める (決めること 1・5)。
4. 指名の検出方式 (決めること 2) と、次話者・重み内訳のフロント提示 (決めること 3) を、SillyTavern natural order の調査 (_sandbox/sillytavern-natural-order.md) を比較材料として整理する。turnOrder.ts がターン開始前に話者を予測している制約を踏まえる。
5. docs/multi-agent-chat-design.md に §4.6 (規則本体) と §8.2 の整合を追記。§2 の用語・§7 の段階分けも更新する。
6. 決めること 4: 実装タスクを起票する (または不要の根拠を記録)。TASK-21 の trpg-table プリセットを検証シナリオに含める。
7. go test ./... と frontend の lint/build を通してから branch-and-commit → pr-loop (reviewer: codex 固定)。
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
spike の成果は `docs/multi-agent-chat-design.md` §4.6 (9 小節)。判断の根拠は
`tools/turn-score-sim` の測定で、`go run ./tools/turn-score-sim` で再現する
(呼びかけの発生だけ固定 seed の乱数、他は決定的)。

## 結論: スコア方式 (`weighted`) を採用 (ユーザー判断)

対案として、呼びかけ先を最優先する係数なしの順位規則 (`addressed_first`) も書いて測った。
**呼びかけ先が 1 人のあいだ、両者は同じことをしている** (追従率 17/17 対 22/22、他の観測量の差は
進行役の間隔 1〜3 対 1〜1 のみ)。差が出るのは 1 発言が 2 人以上を呼ぶ場合で、順位規則は一方を
捨てるしかなく、捨てられた側の待ちが 14 ターン・一巡ターン数 17 に伸びる。スコア方式は
呼びかけ先の全員が ×0.5 を免れるので、待ち 5 ターン・一巡 8 に収まる。
「A さんと B さん、どう思う？」は普通に起きる発言なので、これが決め手になった。

## 起票時の見込みと違っていた点

- **呼びかけの集中 ×0.5 は「重みへの影響」ではなく勝敗の決定として働く。** 呼びかけ先は最低
  0.85×0.95 = 0.8075、他の最大は 0.5 なので、他の 3 係数が何をしようと呼びかけ先が勝つ。
  呼びかけ先を上げる形に書き換えて上げ幅を下げると、境界は 1/(0.85×0.95) ≈ 1.238 にあり、
  ×1.3 で 17/17、×1.2 以下で 6/19 に落ちる。係数が離散値の固定集合なので比較が階段関数になり、
  連続的な影響力は持てない。よって ×0.5 より強める意味はない。
- **起票時に予想した「係数から進行役が 1 ターンおきに挟まる」は再現しない。** 実際は間隔 1〜3 の
  不規則で、`facilitator_alternating` の 1〜1 にも `round_robin` の 3〜3 にもならない中間。
  この違いを作っているのは直近発言の抑制 (×0.85) と進行役の免除だけで、どちらを外しても
  `round_robin` と完全一致する。
- **起票時に「沈黙優遇」が担うと書かれていた「しばらく黙っていた人に話が回る」性質は、実際には
  同点規則が担っていた。** 同点を名簿順で割ると一巡ターン数が 12 まで伸び、沈黙が長い側を採ると 7 に収まる。
- **呼びかけが無く進行役も居ない編成では、スコア方式は `round_robin` と 1 発言も違わない。**
  `weighted` を選ぶ意味があるのは、呼びかけが起きる編成か進行役が居る編成である。

## 語彙

起票時の「指名」は既に 2 つの対象 (`manual` の人間指名、§7 の進行役指名) に使われていたので、
発言本文が相手の表示名を挙げることには **呼びかけ** を与えた。保存先は単数の
`nominated_participant_id` ではなく、**参加者 id の集合**とした (複数呼びかけを表現できることが
`weighted` を採る理由そのものなので、単数に切り詰めない)。「沈黙優遇」は積の定義では係数を
掛けない側なので、実際に係数が掛かる **直近発言の抑制** と呼び替えた。
対応表を本文より先に確定してから書いている (CLAUDE.md の Semantic discipline)。表は 3 版まで改訂した
(連続発言率の定義訂正、呼びかけの単数→複数、採用案の入れ替え)。

## 決めること 3 (次話者と重みの内訳の提示)

`speaker` イベントをターンの SSE に足し、話者と重みの内訳を載せ、`turnOrder.ts` の複製を消す。
位置は `endpointAccepts` の直後に限られる (`handleRunTurnStream` は最初の delta まで SSE ライタを
開かず、それがターン前の失敗を HTTP ステータスで返すための作りである)。
**409 / 404 / 400 / 502 は残るが、生成そのものの失敗の HTTP 500 はストリーム内 `error` に変わる。**
同じ失敗が「delta が 1 つでも届いたか」で 500 とストリーム内 error に分かれる現在の挙動より揃うので
改善として受け入れ、TASK-33 の検証項目に固定した (この点は Codex レビューの [P2] 指摘で修正)。

## 決めること 5 (人間の介入の後)

人間の発言を「現ターンの発言者」とみなし、連続発言の抑制 ×0.2 を誰にも課さない。
×0.2 を課し続けると、人間が直前の話者を名指しで呼んでも 0.2×0.85×0.95 = 0.1615 にしかならず
他の最大 0.5 に負け、追従率 0/12 になる。現ターンの発言者とみなすと 0.8075 で 12/12。
「介入後は常に進行役」も同じ理由で 0/12 なので、TASK-21 の答えとはこの 1 点で揃わない。
名指しの無い介入に限れば両者の結果は近い (一巡 7 対 9、追従 18/20 対 19/20)。

## 起票 (AC #4)

- TASK-33: SSE で話者と重みの内訳を通知し `turnOrder.ts` の複製を消す (§4.6.6)。`weighted` と独立に価値がある。
- TASK-34: `weighted` と呼びかけの検出・保存 (§4.6.1・§4.6.4・§4.6.5・§4.6.7)。TASK-33 に依存。

両方とも「既存の round_robin / manual / facilitator_alternating は変えない」を前提として明記してある。

## 検証

`go build ./...` / `go vet ./...` / `go test ./...` / `pnpm check:client`。
`tools/turn-score-sim` は `package main` なので `go build ./...` に乗り、壊れたままにならない。
実機確認は行っていない (アプリのコードを変更していないため)。§4.6.5 の検出方式の当たり方だけは
小型モデルでの実測が要るが、それは TASK-34 の検証項目に含めた。

Codex レビュー (PR #30 第 1 巡) の [P2] 3 件はすべて修正した: シミュレータの同点規則で未発言の
参加者が負けていた点、呼びかけ追従率が未観測の呼びかけを失敗として数えていた点、上記の HTTP
ステータスの記述。前 2 件は測定値に影響するので、表の数値を測り直して差し替えた。

## 追記 (2026-09-22、Codex レビュー第 2 巡の [P2] 4 件): 採用理由を訂正した

4 件すべて正当だった。うち 2 件は、いったん記録した根拠そのものを崩すものだったので訂正した。

- **シミュレータの「採用」行が仕様と違っていた**: 名指しの無い介入で進行役を強制する上書きを掛けており、
  §4.6.7 が書いている「重みで決める」形を測っていなかった。仕様どおりの行を足して測り直し、
  上書きは別行として残したうえで**採らない**と決めた (§4.6.7)。
- **複数呼びかけ待ちが未応答を取りこぼしていた**: 応答されなかった呼びかけを最大値から外していたため、
  誰も答えなかった transcript が「待ちが短い」と読めていた。未応答件数を併記する形に直した。
- **「順位規則は呼びかけ先の一方を捨てるしかない」は誤りだった**: 比較用に書いた実装が集合の先頭を
  採っていただけで、順位規則一般の限界ではない。呼びかけ先の集合から沈黙が長い側を採る順位規則を足して
  測ると、係数も同点規則も持たずに追従率 30/30・未応答 0・同点 0 を出す。`weighted` は待ちの最大値
  (5 対 7) と一巡ターン数 (8 対 10) で勝つが、28 件中 2 件を未応答で終え、同点規則も要る。
  **測定上は互角で、どちらも一方的に優れてはいない。**
- **呼びかけ係数の閾値の解釈が誤りだった**: 1.238 は「最悪の重みの組み合わせでも勝つ」十分条件の境界で、
  上下で切り替わるスイッチではない。境界を下回っても追従は段階的に落ちるだけで (×1.2 は呼びかけ 3 割・
  介入なしで 17/17、介入ありで 16/20、×1.1 で 12/20 と 6/19、係数なしで 5/19)、数値も scenario を
  取り違えていた。

**その結果、`weighted` を採る理由を書き換えた。** 測定上の優位ではなく、**呼びかけの強さを他の係数と
同じ土俵で調整できる表現力**にある (順位規則には「呼びかけが常に最優先」の一点しかなく、この調整幅が無い)。
採用そのものはユーザー判断として変わらず、既定は原案の係数 (呼びかけを必ず通す設定) とした。

また **どちらの規則も、応答されなかった呼びかけを次の 1 ターンより先へ持ち越さない**ことを明記した。
`weighted` の「呼びかけ先の全員が ×0.5 を免れる」は、その 1 ターンで誰が話すかを決めるだけで、
2 人目に順番を保証しない。持ち越しは §2 の「サーバー側に進行状態を持たない」と両立する導出の設計が
別途必要なので、TASK-34 の範囲外として明記した。

## 追記 (2026-09-22、ユーザー提案「直近 n ターンに呼びかけがある場合 ×1.2」を採用)

呼びかけの係数を「末尾のメッセージの呼びかけ先以外を ×0.5」から
「**直近 n 参加者発言の窓のうち未応答の呼びかけを持つ参加者に ×1.2**」へ替えた。応答したら加点は消える。
これは起票時の原案より測定上優れており、それまで「範囲外」としていた穴を閉じる。

- **応答されない呼びかけが無くなる**: 複数呼びかけで未応答 2/40 → **0/45**、名指しの無い介入がある
  シナリオで 1/20 → **0/20**。窓なしの形は呼びかけを末尾のメッセージ 1 つからしか読まないので、
  `[A, B]` の A が話した瞬間に B への呼びかけが視界から消えていた。
- **一巡ターン数と発言の偏りも改善**: 介入ありで一巡 10 → **5**、複数呼びかけで 8 → **7**・
  最大発言シェア 35% → **33%**。呼びかけが即時に勝ち切らなくなった分、沈黙と名簿順が働く余地が戻る。
- **即時追従率は下がる** (複数呼びかけで 25/25 → 17/27) が、応答待ちの最大値は 4 ターンで未応答 0。
  呼びかけは失われたのではなく 1〜数ターン遅れて通っている。この区別のため観測量を分けた
  (「呼びかけ追従率」→「即時追従率」、「応答待ち」を新設、「最大発言シェア」も追加)。
- **×1.2 には範囲としての意味がある**: `1/0.85 ≈ 1.176` 以下では直近発言の抑制が加点を打ち消して
  呼びかけが届かず (×1.1 で即時追従率 7/19)、`1/(0.85×0.95) ≈ 1.238` 以上では即時に勝ち切って
  窓が働かなくなる (×1.5 と ×2.0 の結果が一致)。×1.2 はこの帯の中にある。
  この帯を §4.6.4 で実装上の制約として明記した。
- **「応答でも残ると居座るのではないか」はユーザーの指摘どおり過剰な懸念だった**。最大発言シェアを
  測ると、残る形でも首位は応答した参加者ではなく進行役 (37%) で、窓なしの形 (35%) と変わらない。
  ×0.2 が直後の再登板を止め、×0.85 が加点 1.2 を 1.02 まで削るためである。それでも「消える」形の方が
  測定上ほぼ全面的に優る (一巡 7 対 9、シェア 33% 対 37%、同点 0 対 2) ので消える形を採った。
- 公平な比較のため、**窓を与えた順位規則** (窓のうち未応答のものから沈黙が長い側を採る) も測った。
  即時追従率では順位規則が勝ち (27/29 対 17/27)、一巡ターン数 (7 対 10)・未応答 (0 対 1)・
  最大シェア (33% 対 35%) では `weighted` が勝つ。互角で傾向が違う。

## 追記: SillyTavern の natural order との比較と `talkativeness` (§4.6.10)

ユーザーの質問に答えて調べ、**`talkativeness` は入れないと決めた**。

決定的なエンジンでは参加者ごとの発言頻度は定数係数にしかならず、SillyTavern の確率とは故障の仕方が違う。
抽選なら 0.2 でも 5 回に 1 回は当たるが、定数係数では構造的に勝てない参加者が生まれる。
編成 GM/A/B/C で C だけ下げて測ると、0.8 で一巡 22、0.5 で一巡 21、**0.2 で一巡せず** (未応答 8/18)。
「控えめ」を設定した利用者が得るのは「無言」になる。入れるなら沈黙を連続量にする変更と抱き合わせが必要で、
それは係数 1 つの追加ではなく規則の形の変更なので、必要になった時点で別に設計する。

副産物として、**進行役の ×0.85 免除は `talkativeness` 1.15 と測定上等価**であることが分かった
(免除を外して進行役に 1.15 を与えると 4 人編成で全指標が一致する)。つまり `weighted` には既に
参加者ごとの発言頻度が 1 つコードに焼き込まれている。将来 `talkativeness` を入れるなら免除を
データ側へ移せるが、今やっても測定上は何も変わらないので TASK-34 の範囲外とした。

応答者リスト方式 (1 リクエストで複数人が話す) も移さない。1 リクエスト = 1 ターンは §4.1 の決定であり、
リストを待ち行列として保存するとサーバー側に進行状態を持つことになって §2 に反する。呼びかけの窓は、
同じ効果を進行状態なしで得るための形である。

対応表は第 4 版まで改訂した (呼びかけの係数の向きが変わったため候補語を差し替え、
「呼びかけの窓」「未応答の呼びかけ」を新語として追加、観測量 3 つを改称・追加)。

## 追記 (2026-09-22、Codex レビュー第 4 巡の [P2] 4 件): 測定の取り違えを修正

4 件すべて正当だった。うち 2 件はシミュレータの実バグで、**設計書に載せた数値が誤っていた**。

- **「採用」と名付けた行が旧規則を走らせていた。** `weighted(proposed, ...)` は起票時の係数
  (末尾のみ・他を ×0.5) であり、窓つきの形ではなかった。窓つきの行は別に並んでいたので、
  §4.6 の表は行によって新旧が混ざっていた。採用案を単一の基準行として定義し直した。
- **窓の大きさが 4 固定だった。** 設計書は「窓 = 編成の人数」と書いているのに、3 人編成でも 4 が
  使われていた。名簿人数を指す値 (-1) に替えた。
- **加点の比較で応答方針がずれていた。** 引用した ×1.1 の行だけ「応答でも残る」で、採用案は
  「応答で消える」だった。変種は一度に 1 つだけ変える形に組み替え、全部測り直した。
- **編成の全員への呼びかけを捨てる扱いは、加点の形では中立でない。** 応答した参加者は加点を失い、
  未応答の参加者は保つので、捨てた場合と残した場合で発言順が変わる。フィルタを外した
  (残すほうが「みなさん、どうですか」で編成が一巡する挙動になる)。
- **AC #5 が遅れた応答を許していなかった。** 名指しの介入でも即時追従は 11/12 で、1 件は 1 ターン遅れる
  (進行役が名簿順の次に当たると 1.0 が呼びかけ先の 0.969 を上回る)。AC を「窓のうちに話す」に直し、
  §4.6.7 の「12/12 のまま」という記述も訂正した。

## 訂正: 加点の下限の正体

「下限 1.176 を下回ると直近発言の抑制が加点を打ち消す」と書いていたが、これは誤りだった。
定常状態では編成の全員が ×0.85 を受けているので、打ち消し合って相対差は消えない。
**下限を作っているのは進行役の免除である**: 呼びかけられた参加者 (0.8075 × 加点) が越えなければ
ならないのは、免除された進行役の 0.95 (名簿順の次でないとき) または 1.0 (次のとき) であり、
そこから 1/0.85 ≈ 1.176 と 1/(0.85×0.95) ≈ 1.238 が出る。
したがって**進行役が居ない編成ではこの境界が効かない** (3 人・進行役なしでは ×1.1 でも即時追従 13/13)。
また 2 つの数は保証の境界であって挙動の切り替わり点ではない (下回っても勝つ場合があり、
上回っても窓は働き続ける)。×1.2 を採る根拠は帯ではなく測定である
(×1.1 は即時追従 5/18、×1.3 以上は複数呼びかけで未応答 2 件、×1.2 だけが未応答 0)。

## 訂正: talkativeness の締め出しは想定より早く起きる

旧規則の数値を引用していたため、締め出しの開始を 0.2 と書いていた。採用案で測り直すと
**0.5 の時点で一巡に加わらなくなる** (未応答 5/11)。0.8 でも一巡 24 (採用案は 7)。
「少し控えめ」のつもりの値でその参加者が会話から落ちるので、入れない判断はより強くなった。

## 追記 (2026-09-22、Codex レビュー第 5 巡): [P2] 1 件・[P3] 1 件

- **[P2] AC #2 が「編成全員への一致は呼びかけ無し」を残していた。** 第 4 巡でフィルタを外す判断をして
  設計書と TASK-34 の説明文は直したが、AC の文面を直し忘れていた。実装者は AC を満たすために
  外したはずのフィルタを実装することになる。AC を「一致した参加者は自分自身を除いて全員が
  呼びかけ先として保存される (編成の全員が一致した場合も捨てない)」に差し替えた。
- **[P3] 数値の転記誤り 2 箇所。** 介入ありの表で、窓つき順位規則の応答待ちを「21 件・未応答 0」と
  書いていたが実測は「未応答 1/22」。また §4.6.7 で、即時追従率 9/12 を「名指しの無い介入だけ
  進行役を強制する形」に帰していたが、9/12 は「名指しの有無によらず強制する形」の値で、
  前者は 11/12 のまま変わらない。読み取り 6 の未応答の比較もシナリオごとの値に直した。

## 追記: §4.6.7 の上書きの評価を訂正 (自己発見)

第 6 巡の途中で Codex が利用上限に達して判定は出なかったが、その実行ログに含まれていた測定値から
自分の記述の誤りが 2 つ見つかったので直した。

- 「名指しの無い介入のときだけ進行役を強制する上書きは、得られるものが無い」と書いていたが誤り。
  実測では**即時追従率 9/20 → 16/20 の利得**があり、代わりに一巡ターン数 5 → 8、未応答 0/20 → 1/21、
  同点 2 → 4 を失う。損得のある取引である。入れない理由を「失うもの (未応答 0 と短い一巡) が、
  窓つきの加点を採った理由そのものだから」に書き直し、この行を 4.6.3 の表にも出した。
- 同じ段落に「12/12 のまま」という旧値が残っていた (第 5 巡の修正が隣の段落に当たっていた)。
  段落そのものが逐次修正で矛盾を重ねていたので書き直した。

## 追記 (2026-09-22、ユーザーの指摘「名簿順の次以外 ×0.95 は必要か」): 係数を 3 つに減らした

**冗長だったので外した。** 測定すると、呼びかけの無い編成ではこの係数を戻しても全指標が一致し、
変わるのは同点の回数だけだった (4 人・進行役ありで 0 対 16、3 人で 0 対 59)。同点規則の
「沈黙が長い側」がこの係数と同じ参加者を指すので、係数は同点を事前に潰していただけだった。

一方で**害があった**: 呼びかけられた参加者が名簿順の次でないときこの係数を受けるので、
呼びかけの必要加点が `1/0.85 ≈ 1.176` から `1/(0.85×0.95) ≈ 1.238` に上がっていた。
外すと境界が 1 本になり、**×1.2 で足りる**。戻すと即時追従率が 21/21 から 14/21 に落ちる。

これで前巡まで争点だった「×1.2 か ×1.3 か」が消えた。**×1.2 / ×1.3 / ×2.0 は全シナリオで完全に一致する**
(1.176 を越えた時点で飽和する)。「帯」という込み入った説明も要らなくなった。

副産物として、この規則が実際に行っていることが正確に書けるようになった:
**重みは粗いふるいで、順序は同点規則が決めている。** 係数が 3 つとも離散値なので重みが並ぶことが多く
(60 ターン中 16〜21 回、進行役の居ない 3 人編成では 59 回)、係数が担っているのは
「直前の話者を外す」「呼びかけられた参加者を持ち上げる」「進行役を戻す」の 3 つだけである。
同点規則を名簿順だけにすると一巡 8 → 12・最大シェア 32% → 40% に悪化するので、中核は同点規則の側にある。

進行役の免除の等価係数も `1/0.85 ≈ 1.176` に変わった (×0.95 が無くなったため)。免除を外して
進行役に 1.176 を与えると、呼びかけの無い 4 人編成で全指標が一致する。

## 第 6 巡 (Claude Code CLI へフォールバック) の [P1]・[P2] 4 件・[P3] 4 件

Codex が利用上限に達して判定を出せなかったため、pr-loop のガードレールに従い Claude Code CLI に
1 巡だけフォールバックした。9 件すべて正当で、うち 2 件は測定手法の欠陥だった。

- **[P1] 未応答の件数が打ち切り時の残件を数えていた。** 応答率には第 1 巡で末尾補正を入れたのに、
  待ち時間の指標には入れ忘れていた。`×1.3` では呼びかけの最悪重みが `1.0498 > 1.0` なので構造的に
  飢餓が起きえず、「未応答 2 件」は 60 ターン目で消化中だった残件だった。全員呼びのシナリオが
  決定的な証拠で、60 ターン目の介入は後続が 1 発言・呼びかけ先が 4 人なので構造上 3 件が計上される。
  **これにより「×1.2 だけが未応答 0」という ×1.2 採用の根拠が消えた** (その後 ×0.95 を外したことで
  論点自体が解消した)。
- **[P2] 規則ごとに違う呼びかけを相手にしていた。** 抽選が話者選択の後に走り「話者以外」を棄却抽選して
  いたため、消費する乱数が誰が話したかに依存していた。同一シナリオの行が 17〜22 件の呼びかけを
  相手にしており、22/22 と 17/17 を比べていたのは同じ試験ではなかった。シナリオごとに一度抽選し、
  話者からのオフセットとして持つ形に替えた。これにより `addressed_first` の数値が軽い負荷で
  下駄を履いていたことも判明し、公平な抽選では一巡ターン数 30 (採用案は 8) と明確に劣る。
- [P2] §4.6.5 が全員一致フィルタを外した根拠に読み取り 9 を引いていたが、読み取り 9 はそれを否定していた。
  機構 (応答した参加者は加点を失い、未応答の参加者は保つので時間が経つと差が付く) を理由として書き直した。
- [P2] 窓の大きさが無測定の主張だった。窓 2 / 4 / 8 を測ると全シナリオでほぼ一致するので、
  「大きさを選ぶ測定上の理由は無い」と正直に書いた。
- [P2] talkativeness の基準行が別シナリオ・別 seed だった。同一 seed の基準行を足して測り直すと、
  締め出しの開始は 0.5 ではなく **0.8** (一巡 7 → 39) だった。
- [P3] 進行役の免除と係数の等価性が測定範囲を超えて書かれていた / `secondCallWait` のコメントが
  コードと違う metric を説明していた / 表の 2 セルが件数を落としていた / 上書きの代償の列が不足していた。
  すべて修正。

## 追記 (2026-09-22、第 7 巡の [P2] 4 件・[P3] 7 件): 窓の根拠を測り直した

- **[P2] 書き直しの過程で「一度に 1 つだけ変える」を自分で破っていた。** 窓の効果を「起票時の原案」との
  比較で語っていたが、原案は窓と名簿順の 2 つが同時に違う。しかも窓だけを外した行を書き直しのときに
  削っていた。**窓の正当化が測定できていない状態だった。**
  「窓なし (末尾のみ)・他は採用と同じ」を戻して測ると、**窓が効くのは呼びかけ先が複数のときと
  人間の介入があるときだけ**で、呼びかけ先が 1 人のあいだは 1 発言も違わない。
  複数呼びかけで最大シェア 32% 対 40%・複数呼びかけ待ち 4 対 6、4 人+介入で即時追従 20/20 対 19/20・
  応答待ち 1 対 2、3 人+介入で 15/15 対 13/15・1 対 3、全員呼びでシェア 35% 対 40%。
  §4.6.4 の根拠をこの行に置き換えた。「係数が 4 つから 3 つに減る」は名簿順の除去による別の変更なので、
  窓の根拠から外した。
- **[P2] 加点の境界を「進行役の免除」だけに帰していたのは不正確だった。** 名簿順の係数が無くなった後は、
  窓を越えて沈黙した参加者も重み 1.0 に上がる (×0.85 を受けなくなる) ので、越える相手は
  「重み 1.0 の参加者」である。したがって**進行役が居ない編成でも境界は無くならない**
  (3 人・進行役なしで、3 発言沈黙した参加者の 1.0 が、直近に発言した呼びかけ先の 0.935 に勝つ)。
- **[P2] 読み取り 6 の「窓を与えた変種は改善する (一巡 10)」が自分の表と矛盾していた。** 一巡 30 の行は
  「3 変種すべて一致」なので窓つきも 30 である。窓が直すのは待ちの側だけと書き直した。
- **[P2] talkativeness の 0.5 / 0.2 が別 seed のままだった。** 基準行だけ揃えて「全て同じ seed」と
  書いていた。全行を seed 9 に揃えて測り直した (0.5 / 0.2 は未応答 3/10)。
- [P3] 7 件: 係数の数の記述が 3 箇所 4 のままだった / 「呼びかけの無い編成では round_robin とほぼ同じ」が
  直下の表と矛盾 / 呼びかけ件数が表頭 19 と列 21 で食い違い、件数だけの記法の規約が未記述 /
  「全指標が一致」が 7 指標中 3 指標での主張で、同点は上書き経路が重みを返さないため原理的に比較不能 /
  検出 (b) の根拠が窓の大きさの読み取りを引いていた / 等価性の caveat の後に「つまり」が続いていた /
  ツールのコメントと `tweak` ヘルパ。すべて修正。

**`turn 0` の抽選を消す [P3] は採らなかった。** 消すと以降の乱数消費がずれて §4.6.3 の全数値が動く。
規則の振る舞いは何も変わらないのに節を作り直すことになるので、意図的に残してコメントに理由を書いた。

## 追記 (2026-09-22、第 8 巡): [P1]・[P2] なし、[P3] 5 件を修正して収束

レビュアーが「[P1]・[P2] は残っていない。5 件の [P3] はラベルと相互参照の問題で、もう 1 巡を要するものではない」
と明記した。5 件はすべて直した。

- 全員呼びのシナリオの 2 行を、読み取り 9 では「窓の効果」、§4.6.5 では「加点の有無の差は小さい」と
  逆向きに性格づけていた。**このシナリオでは呼びかけがその介入しか起きないので、窓なしと加点なしが
  同一の規則になる**ことを明記し、§4.6.5 の「差は小さい」も撤回した (シェア 35% 対 40% は、
  複数呼びかけの 32% 対 40% と同じ程度である)。
- 「複数呼びかけ待ち 4 対 6」と書いていたが、書き直しでその列を表から外していたので照合できなかった。
  値は応答待ちのものなので名前を直した。
- 3 人・進行役なし + 介入のシナリオが表に無いまま §4.6.4 の根拠にしていたので、表を足した。
- `withBoost` が `callBoost` だけを設定しており、末尾のみの形では読まれない (そちらは `addresseeBoost`)。
  今の行に誤りは無いが、将来 `withBoost(lastMessageOnly, 1.1)` の行を足すと ×1.2 を測って ×1.1 と
  ラベルすることになる。両方を設定するようにした (出力は committed 版と diff 0 で不変を確認)。
- 応答待ちの欄の記法説明が「件数だけを書いてある」と書いていたが、素の数字はターン数である。
  表頭の呼びかけ件数のラベルも 26 と 39 が入れ替わっていた。

**転記誤りが 8 巡で繰り返し出たので、引用した数値をツールの出力と機械照合する手順を入れた。**
この巡は 12 点を照合して不一致 0。TASK-34 に着手するときは `tools/turn-score-sim` にテストを書くのが
先だと考える (`tools/` に `_test.go` が無いことが、この spike で結論が 3 回動いた直接の原因である)。
<!-- SECTION:NOTES:END -->
